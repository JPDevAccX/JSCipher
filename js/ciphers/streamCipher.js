import Cipher from "./cipher.js";

export default class StreamCipher extends Cipher {
	// No functionality here yet but kept for typing
	// A function to generate a buffer with the keystream could be added (e.g. so it can be displayed)
}